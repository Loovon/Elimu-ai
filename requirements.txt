# Elimu AI - clean production/runtime requirements
# Preserves the existing FastAPI + Gemini + Qdrant + PostgreSQL + HTTP + APScheduler architecture.
# Excludes the old full-environment freeze, venv, Django/frontend packages, CUDA/NVIDIA stack,
# and local ML inference packages.

# Web/API
fastapi==0.115.14
uvicorn[standard]==0.49.0
gunicorn==26.0.0

# Gemini
google-genai==1.31.0
protobuf==5.29.5
tenacity==9.1.4

# Qdrant / RAG
qdrant-client==1.14.3

# PostgreSQL memory / analytics
psycopg2-binary==2.9.12

# Elimu AI <-> ElimuTalks HTTP boundary
httpx==0.28.1
requests==2.34.2

# Configuration
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1

# Background worker / scheduler
APScheduler==3.11.3
tzlocal==5.4.4

# Elimu Library catalog / document ingestion
beautifulsoup4==4.15.0
pypdf==5.4.0
lxml==6.1.1

# Runtime utilities
PyYAML==6.0.3
orjson==3.11.9
python-dateutil==2.9.0.post0
regex==2026.7.19
rich==15.0.0

# IMPORTANT:
# Do not add venv/ to Git.
# Do not add .env or production secrets to Git.
#
# Intentionally excluded:
# Django / django-allauth / django-crispy-forms / crispy-bootstrap5
# CUDA / NVIDIA / triton
# torch / sentence-transformers / transformers
# Anthropic SDK
# Old Ollama/Chroma-only dependencies
#
# Gemini production configuration:
# GEMINI_MODEL = existing production model
# EMBED_MODEL = gemini-embedding-001
# EMBED_DIM = 768
#
# The google-ai-generativelanguage package is intentionally not pinned here.
# google-genai is the current Gemini SDK used by the production architecture,
# and omitting the old direct pin avoids its protobuf<6 conflict.
